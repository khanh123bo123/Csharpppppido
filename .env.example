API_PORT=8080

POSTGRES_DB=tourguide
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

JWT_KEY=replace-with-a-long-random-secret-key-at-least-32-characters
JWT_ISSUER=TourGuideApi
JWT_AUDIENCE=TourGuideApp

# Required if you use localization translation via Gemini.
GEMINI_API_KEY=

# Comma-separated allowed origins for browser clients.
ALLOWED_ORIGINS_CSV=http://localhost:3000,http://localhost:3001
